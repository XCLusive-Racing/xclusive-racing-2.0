memory_limit = 256M
upload_max_filesize = 200M
post_max_size = 205M
max_execution_time = 120
max_input_time = 120
; Bulk race import/generator posts ~20 fields per row -- the default 1000 silently
; drops every row past ~48. 5000 covers the 200-row cap bulkStore() validates.
max_input_vars = 5000
